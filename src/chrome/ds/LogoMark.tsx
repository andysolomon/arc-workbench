// Typegram design-system LogoMark (components/surfaces/LogoMark.jsx), typed. Identical DOM.
export function LogoMark({ size = 34 }: { size?: number }) {
  return <div className="tg-logo" style={{ width: size, height: size, borderRadius: Math.round(size * 0.26), fontSize: Math.round(size * 0.41) }}>{'{ }'}</div>;
}
